# Production API (no trailing /api). Used at build time for EAS production/preview profiles.
EXPO_PUBLIC_API_URL=https://portal.easyfuel.ai
EXPO_PUBLIC_API_BASE_URL=https://portal.easyfuel.ai

# Optional WebSocket override (default: same host as API, path /ws)
# EXPO_PUBLIC_WS_URL=wss://portal.easyfuel.ai/ws
